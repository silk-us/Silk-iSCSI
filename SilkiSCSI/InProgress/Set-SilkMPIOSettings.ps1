function Set-SilkMPIOSettings {
    param()

    # Process

    # Set any incorrect Silk MPIO settings

    # Ask for each setting in the param() list

    # Loop through each setting and only set it if it's drifted. 

    # Return a table showing:
    # MPIO policy setting, before, after, requires restart
}